pluginManagement {
    plugins {
        id 'org.springframework.boot' version '2.7.18'
    }
}

rootProject.name = 'common'

include 'oh-adapter'

if (DEBUG_MODE == 'true') {
    include 'oh-common'
}
